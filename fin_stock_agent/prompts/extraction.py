TRADE_EXTRACTION_PROMPT = """从用户的对话消息中提取股票买卖交易记录。

## 任务
仔细阅读用户消息，找出所有明确提及的股票买入或卖出行为。

## 输出格式
如果找到交易记录，输出严格 JSON 数组（不加任何其他文字）：
```json
[
  {{
    "ts_code": "股票代码，如 600519.SH（若只有名称则填名称，系统会自动查询代码）",
    "name": "股票名称（若有）",
    "direction": "buy 或 sell",
    "quantity": 数量（数字），
    "price": 价格（数字），
    "fee": 手续费（数字，若未提及则填 0），
    "trade_date": "YYYYMMDD 格式（若未提及则填 unknown）"
  }}
]
```

如果**没有**明确的买卖交易记录，输出：
```json
[]
```

## 提取规则
- 必须有明确的买入或卖出动作词（买了、购入、卖了、卖出、清仓等）。
- 必须有数量和价格，缺一则跳过该条。
- 「关注」「看好」「想买」等意向词不算实际交易，不提取。
- 代码格式：沪市6开头加.SH，深市0/3开头加.SZ。若无法判断交易所，保留原始代码/名称。

用户消息：{message}"""
